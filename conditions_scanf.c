#include <stdio.h>
#include "stdlib.h"

int main(int argc, char *argv[])
{
    
    int quelAgeATu;
    int quelEstTonArgent;
    
    if (age > 30 || argent > 100000)
        
    {
        printf("Quel age avez vous? ?");
                scanf("%d", &quelAgeATu);
        printf("Bienvenue chez PicsouBanque !\n");
    }
    else
    {  printf("Quel age avez vous? ?");
        scanf("%d", &quelEstTonArgent);
        printf("Hors de ma vue, miserable !\n");
    }
    return 0; }
